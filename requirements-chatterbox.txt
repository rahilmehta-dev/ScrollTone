# ── Chatterbox engine (optional) ───────────────────────────────────────────────
# Install into a SEPARATE venv, not the main .venv — this stack (torch/diffusers
# pins) is independently validated and may not coexist cleanly with Kokoro's or
# Higgs's dependency versions.
#
#   python -m venv .venv-chatterbox
#   .venv-chatterbox/bin/pip install -r requirements-chatterbox.txt
#
# Model weights (~3GB) download from HuggingFace on first use.
#
# IMPORTANT: this app always runs Chatterbox on CPU (backend/engines/
# chatterbox_synth.py hardcodes device="cpu"), never MPS/CUDA. Chatterbox's
# autoregressive decoder has a confirmed, severe memory leak on Apple's MPS
# backend (grew past 78GB RSS in testing). Do not override this without
# re-validating from scratch.
#
# See README "Optional: Higgs Audio V2 / Chatterbox engines".

chatterbox-tts>=0.1.7
torch>=2.6.0
torchaudio>=2.6.0
